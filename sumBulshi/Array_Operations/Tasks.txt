Array Operations

Arrays in C are contiguous memory. Tasks may test indexing and pointer iteration. For example, reversing or rotating elements.

    Task: Write a C function to reverse an integer array in place (swap first/last, etc.)
    geeksforgeeks.org
    . (Concept: array indexing and swapping)

    Task: Rotate a C array of integers left by k positions (e.g. {1,2,3,4,5} with k=2 becomes {3,4,5,1,2})
    geeksforgeeks.org
    . (Concept: array manipulation with loops)

